[package]
name = "ghostwriter"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"
description = "Local speech-to-text dictation for the COSMIC desktop"
repository = "https://github.com/hmrdsmoke/GhostWriter"

[build-dependencies]
xdgen = "0.1"

[dependencies]
i18n-embed-fl = "0.10"
rust-embed = "8.11"
tokio = { version = "1.52.0", features = ["full"] }
# Microphone capture.
cpal = "0.18"
# Whisper inference (whisper.cpp bindings). CPU-only until the pipeline is proven.
whisper-rs = "0.16"
# GPU builds — replace the line above with ONE of these:
#   whisper-rs = { version = "0.16", features = ["cuda"] }    # NVIDIA, needs the CUDA toolkit (nvcc)
#   whisper-rs = { version = "0.16", features = ["vulkan"] }  # any GPU, needs libvulkan-dev + glslc

[dependencies.i18n-embed]
version = "0.16"
features = ["fluent-system", "desktop-requester"]

[dependencies.libcosmic]
git = "https://github.com/pop-os/libcosmic.git"
rev = "4657b6a7259e6ecdbf0af6f956de163e3d49dc1a"
# See https://github.com/pop-os/libcosmic/blob/master/Cargo.toml for available features.
features = [
    # Applet support
    "applet",
    "applet-token",
    # Uses cosmic-settings-daemon to watch for config file changes
    "dbus-config",
    # Support creating additional application windows.
    "multi-window",
    # Uses tokio as the executor for the runtime
    "tokio",
    # Add Wayland support to winit
    "wayland",
    # Windowing support for X11, Windows, Mac, & Redox
    "winit",
]

# Uncomment to test a locally-cloned libcosmic
# [patch.'https://github.com/pop-os/libcosmic']
# libcosmic = { path = "../libcosmic" }
# cosmic-config = { path = "../libcosmic/cosmic-config" }
# cosmic-theme = { path = "../libcosmic/cosmic-theme" }